import DrawModal from '@components/Base/DrawModal';
import type { BaseDrawProps } from '@components/Base/DrawModal';

function FilterDraw(props: BaseDrawProps) {
  return (
    <DrawModal {...props}>
      <div>dfdfd</div>
    </DrawModal>
  );
}
export default FilterDraw;
