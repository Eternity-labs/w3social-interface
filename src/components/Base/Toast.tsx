import Snackbar from '@mui/material/Snackbar';

type ToastProps = {
  isOpen: boolean;
  severity?: 'info';
  message: string;
};
const GlobalToast = () => {
  // const { isOpen, severity = 'info', message } = props;
};
